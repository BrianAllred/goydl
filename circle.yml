machine:
  post:
    - mkdir -p $GOPATH/src/github.com/BrianAllred/goydl

general:
  build_dir: ../.go_workspace/src/github.com/BrianAllred/goydl

dependencies:
  pre:
    - go get github.com/tools/godep

test:
  pre:
    - go vet

  override:
    - godep go test
